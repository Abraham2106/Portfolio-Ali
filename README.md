
  # Portfolio Educadora Layout

  This is a code bundle for Portfolio Educadora Layout. The original project is available at https://www.figma.com/design/r5OIOgrCgDnI4vtakuyM7z/Portfolio-Educadora-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  